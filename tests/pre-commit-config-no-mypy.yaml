default_install_hook_types: [pre-commit, pre-push, commit-msg]

repos:
- hooks: # this should not update (to preserve idiosyncrasies)
          - id: poetry-check
            stages: [pre-commit]

  repo: https://github.com/python-poetry/poetry
  rev: 1.8.3